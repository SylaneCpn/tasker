/// Annotates that the class can be both encoded and decoded to Json.
class Serializable {
  const Serializable();
}

/// Annotates that the class can be both encoded and decoded to Json.
const serializable = Serializable();
