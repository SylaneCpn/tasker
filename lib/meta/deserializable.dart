/// Annotates that the class can decoded to Json with a from Json factory constructor.
class Deserializable {
  const Deserializable();
}
/// Annotates that the class can decoded to Json with a from Json factory constructor.
const deserializable = Deserializable();
